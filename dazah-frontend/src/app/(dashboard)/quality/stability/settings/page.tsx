'use client'

import { Card, Typography } from 'antd'

const { Title, Text } = Typography

export default function StabilitySettingsPage() {
  return (
    <Card>
      <Title level={4}>稳定性实验管理 - 提醒设置</Title>
      <Text type="secondary">功能开发中...</Text>
    </Card>
  )
}
